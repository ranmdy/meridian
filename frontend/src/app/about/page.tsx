import { AboutPage } from '@/src/components/pages/AboutPage';

export const metadata = {
  title: 'About — Meridian',
  description: 'Capital should move to yield, not to us.',
};

export default function About() {
  return <AboutPage />;
}
