import { TermsPage } from '@/src/components/pages/TermsPage';

export const metadata = {
  title: 'Terms of Access — Meridian',
  description: 'Meridian terms of service — self-custody DeFi routing protocol.',
};

export default function Terms() {
  return <TermsPage />;
}
